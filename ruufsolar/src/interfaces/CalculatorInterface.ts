export interface CalculatorArgs {
    dpc: number;
    dsc: number;
    dpp: number;
    dsp: number;
}

export interface LabelsObject {
    dpc: string;
    dsc: string;
    dpp: string;
    dsp: string;
}
